// База знаний о продукте для AI-гейткипера продуктовых запросов.
// Редактируется продуктом руками. Агент опирается ТОЛЬКО на то, что здесь написано:
// если функции нет в списке — он не считает её ни существующей, ни отсутствующей, а спрашивает.

export const KNOWLEDGE = `
## Продукт
Omni 360 DSP — платформа programmatic-закупки наружной цифровой рекламы (DOOH).
Пользователи: медиапланеры агентств, менеджеры рекламодателей, внутренняя команда CS/продаж.

## Основные сущности и процессы
- Рекламодатели → бренды → кампании. Кампания привязана к бренду.
- Типы кампаний: RTB (аукцион), FLEX_GUARANTEED, GUARANTEED.
- Мастер создания кампании по шагам: бриф → параметры (даты, бюджет, наценка, лимиты) → экраны (планировщик по городам/операторам/форматам, POI-радиусы) → креативы → сводка → запуск.
- Статусы кампании: NEW, ON_MODERATION, ACTIVE, STOPPED, COMPLETED, CANCELLED.
- Креативы: загрузка, привязка к кампании, модерация оператором (возможен отказ с причиной).
- Статистика: показы, OTS, расходы по кампаниям; экспорт таблиц в Excel; экспорт медиаплана (МП) по форматам.
- Справочники: города, операторы экранов, форматы, инвентарь экранов.
- Уведомления в интерфейсе.
- ИИ-ассистент по кампаниям и статистике (чат).

## Известные ограничения
- Статистика по кампании доступна суммарно за всё время, детализация по периодам ограничена.
- Экспорт отчётов: Excel есть, PowerPoint/PDF нет.
- Наценка (надбавка) лочится после запуска кампании — это осознанное правило, не баг.

## Продуктовые принципы
- Сначала проблема и её масштаб, потом фича.
- Не строим под одного клиента то, что не переиспользуется.
- Ручной обходной путь допустим для редких случаев, если он не создаёт ошибок и лишней работы CS.

## Ранее отклонённые / отложенные запросы
- (пусто — заполняется продуктом)
`
